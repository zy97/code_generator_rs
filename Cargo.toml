[workspace]
members = ["app", "code_generator", "code_generator_cli","csharp_binding"]
default-members = ["csharp_binding"]

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"   #会影响行为，慎用
