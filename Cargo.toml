[workspace]
members = ["code_generator", "csharp_binding"]
default-members = ["code_generator"]

[profile.release]
strip = true
opt-level = "z"
lto = true
codegen-units = 1
panic = "abort"   #会影响行为，慎用
